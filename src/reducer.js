
const initialState = {
  email: "test@emai.com",
  fname: "",
  lname: "",
  isInvalid: false
}

function reducer(state = initialState, action) {
  switch(action.type) {
    default:
      return state
  }
}

export default reducer
